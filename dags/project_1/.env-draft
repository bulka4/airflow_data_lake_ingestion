# Name of the Storage Account (Data Lake) (no special characters and uppercase letters. Globally unique.)
ACCOUNT_NAME = "youdatalake"

# Data Lake access key. When we are creating a Data Lake using the data_lake module from the azure_terraform repository, the access key 
# is saved in the Terraform output 'primary_access_key'.
ACCESS_KEY = "5ugmtmer5kG6vGnNzM7+QVJ4+2Ttmd+-uJT5IeCFusbuEfLbbIzJsL5fbZ1XFEjJZ7IFFRYZk9oD+AStf/yMhg=="

# Name of the SQL server from which we will be ingesting data.
SQL_SERVER_NAME = "data-source.database.windows.net"

# Name of the db in the SQL server from which we will be ingesting data (it can be any database on that server).
SQL_DB_NAME = "db"

# username and password used for logging into the SQL server.
SQL_USERNAME = "username"
SQL_PASSWORD = "password"